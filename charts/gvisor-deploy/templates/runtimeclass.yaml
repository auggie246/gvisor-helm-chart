{{- if .Values.runtimeClass.create }}
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: {{ .Values.runtimeClass.name }}
  labels:
    {{- include "gvisor-deploy.labels" . | nindent 4 }}
handler: {{ .Values.containerd.runtimeName }}
{{- end }}
